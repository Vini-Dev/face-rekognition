import { FC } from 'react';

import Camera from 'src/components/Camera';

import { Container } from './styles';

const Home: FC = () => (
  <Container>
    <Camera />
  </Container>
);

export default Home;
